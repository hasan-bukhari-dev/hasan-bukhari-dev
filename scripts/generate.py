from pathlib import Path
# Edit this file if you want to regenerate the SVGs programmatically.
# The current assets are already included in the repository.
print("Profile assets are already generated in assets/.")
